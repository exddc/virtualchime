# Virtual Chime - Buildroot Configuration

This directory contains the Buildroot external tree for building a lightweight
Linux image for the Raspberry Pi Zero W that runs the chime application.

## Prerequisites

- Linux build host (native or VM)
- ~10-25GB disk space for Buildroot build
